# RecipeAuthor

## Описание

`RecipeAuthor` отображает автора рецепта и его роль. Компонент принимает `author` и `authorRole`.

## Поведение

Компонент показывает круглый аватар-заглушку с инициалами `АК`, имя автора и подпись роли. Используется внутри верхнего блока рецепта.

## Тесты

Тестов нет.

## Todo

- Формировать инициалы автора из данных, а не использовать статичное значение `АК`.
